import 'package:flutter/material.dart';

class MaintainerProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Maintainer Profile')),
      body: const Center(child: Text('Maintainer Profile Page')),
    );
  }
}
